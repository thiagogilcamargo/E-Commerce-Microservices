{
  "compilerOptions": {
    /* Visite https://aka.ms/tsconfig.json para ler mais sobre este arquivo */

    /* Opções Básicas */
    // "incremental": true,                         /* Habilita compilação incremental */
    "target": "es5",                                /* Especifica a versão alvo do ECMAScript: 'ES3' (padrão), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019', 'ES2020' ou 'ESNEXT'. */
    "module": "commonjs",                           /* Especifica a geração de código do módulo: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', 'es2020' ou 'ESNext'. */,
    // "lib": [],                                   /* Especifica arquivos de biblioteca a serem incluídos na compilação. */
    // "allowJs": true,                             /* Permite a compilação de arquivos JavaScript. */
    // "checkJs": true,                             /* Relata erros em arquivos .js. */
    // "jsx": "preserve",                           /* Especifica a geração de código JSX: 'preserve', 'react-native', 'react', 'react-jsx' ou 'react-jsxdev'. */
    "declaration": true,                         /* Gera o arquivo '.d.ts' correspondente. */,
    // "declarationMap": true,                      /* Gera um sourcemap para cada arquivo '.d.ts' correspondente. */
    // "sourceMap": true,                           /* Gera o arquivo '.map' correspondente. */
    // "outFile": "./",                             /* Concatena e emite saída para um único arquivo. */
    "outDir": "./build",                              /* Redireciona a estrutura de saída para o diretório. */,
    // "rootDir": "./",                             /* Especifica o diretório raiz dos arquivos de entrada. Use para controlar a estrutura do diretório de saída com --outDir. */
    // "composite": true,                           /* Habilita a compilação do projeto */
    // "tsBuildInfoFile": "./",                     /* Especifica o arquivo para armazenar informações de compilação incremental */
    // "removeComments": true,                      /* Não emite comentários para a saída. */
    // "noEmit": true,                              /* Não emite saídas. */
    // "importHelpers": true,                       /* Importa ajudantes de emissão de 'tslib'. */
    // "downlevelIteration": true,                  /* Fornece suporte completo para iteráveis em 'for-of', spread e destructuring ao direcionar 'ES5' ou 'ES3'. */
    // "isolatedModules": true,                     /* Transpila cada arquivo como um módulo separado (similar a 'ts.transpileModule'). */

    /* Opções de Verificação Estrita de Tipos */
    "strict": true,                                 /* Habilita todas as opções de verificação de tipo estrito. */,
    // "noImplicitAny": true,                       /* Gera erro em expressões e declarações com um tipo 'any' implícito. */
    // "strictNullChecks": true,                    /* Habilita verificações estritas de nulo. */
    // "strictFunctionTypes": true,                 /* Habilita verificação estrita de tipos de função. */
    // "strictBindCallApply": true,                 /* Habilita métodos 'bind', 'call' e 'apply' estritos em funções. */
    // "strictPropertyInitialization": true,        /* Habilita verificação estrita de inicialização de propriedade em classes. */
    // "noImplicitThis": true,                      /* Gera erro em expressões 'this' com um tipo 'any' implícito. */
    // "alwaysStrict": true,                        /* Analisa no modo estrito e emite "use strict" para cada arquivo de origem. */

    /* Verificações Adicionais */
    // "noUnusedLocals": true,                      /* Relata erros em variáveis locais não utilizadas. */
    // "noUnusedParameters": true,                  /* Relata erros em parâmetros não utilizados. */
    // "noImplicitReturns": true,                   /* Relata erro quando nem todos os caminhos de código na função retornam um valor. */
    // "noFallthroughCasesInSwitch": true,          /* Relata erros para casos de queda em declarações switch. */
    // "noUncheckedIndexedAccess": true,            /* Inclui 'undefined' nos resultados do índice da assinatura */
    // "noPropertyAccessFromIndexSignature": true,  /* Requer propriedades não declaradas das assinaturas de índice para usar acessos aos elementos. */
     /* Opções de Resolução de Módulos */
    // "moduleResolution": "node",                  /* Especifica a estratégia de resolução de módulos: 'node' (Node.js) ou 'classic' (TypeScript pré-1.6). */
    // "baseUrl": "./",                             /* Diretório base para resolver nomes de módulos não absolutos. */
    // "paths": {},                                 /* Uma série de entradas que remapeiam imports para locais de busca relativos à 'baseUrl'. */
    // "rootDirs": [],                              /* Lista de pastas raiz cujo conteúdo combinado representa a estrutura do projeto em tempo de execução. */
    // "typeRoots": [],                             /* Lista de pastas para incluir definições de tipo. */
    // "types": [],                                 /* Arquivos de declaração de tipo a serem incluídos na compilação. */
    // "allowSyntheticDefaultImports": true,        /* Permite imports padrão de módulos sem exportação padrão. Isso não afeta a emissão de código, apenas a verificação de tipo. */
    "esModuleInterop": true,                        /* Habilita interoperabilidade de emissão entre CommonJS e ES Modules através da criação de objetos de namespace para todas as importações. Implica 'allowSyntheticDefaultImports'. */,
    // "preserveSymlinks": true,                    /* Não resolve o caminho real dos links simbólicos. */
    // "allowUmdGlobalAccess": true,                /* Permite acessar globais UMD a partir de módulos. */

    /* Opções de Mapa de Origem */
    // "sourceRoot": "",                            /* Especifica o local onde o depurador deve localizar os arquivos TypeScript em vez de locais de origem. */
    // "mapRoot": "",                               /* Especifica o local onde o depurador deve localizar os arquivos de mapa em vez de locais gerados. */
    // "inlineSourceMap": true,                     /* Emite um único arquivo com mapas de origem em vez de ter um arquivo separado. */
    // "inlineSources": true,                       /* Emite a origem junto com os mapas de origem dentro de um único arquivo; requer '--inlineSourceMap' ou '--sourceMap' para ser configurado. */

    /* Opções Experimentais */
    // "experimentalDecorators": true,              /* Habilita o suporte experimental para decoradores ES7. */
    // "emitDecoratorMetadata": true,               /* Habilita o suporte experimental para emitir metadados de tipo para decoradores. */

    /* Opções Avançadas */
    "skipLibCheck": true,                           /* Ignora a verificação de tipo de arquivos de declaração. */,
    "forceConsistentCasingInFileNames": true        /* Impede referências com letras maiúsculas e minúsculas inconsistentes para o mesmo arquivo. */
  }
}
